import { RangePriceProductPipe } from './range-price-product.pipe';

describe('RangePriceProductPipe', () => {
  it('create an instance', () => {
    const pipe = new RangePriceProductPipe();
    expect(pipe).toBeTruthy();
  });
});
