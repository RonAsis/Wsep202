import { ProductRankPipe } from './product-rank.pipe';

describe('ProductRankPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductRankPipe();
    expect(pipe).toBeTruthy();
  });
});
