import { StoreRankPipe } from './store-rank.pipe';

describe('StoreRankPipe', () => {
  it('create an instance', () => {
    const pipe = new StoreRankPipe();
    expect(pipe).toBeTruthy();
  });
});
